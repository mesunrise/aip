# 服务器切换指南

## ❌ 不需要更新APK！

### 为什么不需要？

**服务器地址是在App中手动输入的，不是硬编码的！**

App界面有一个"服务器地址"输入框，用户可以随时修改连接地址。

### 📱 如何切换服务器

#### 方法1：在App中直接修改

1. 打开App
2. 找到"服务器地址"输入框
3. 输入新服务器地址：
   ```
   ws://wdzd1450232.bohrium.tech:8080/ws
   ```
4. 点击"连接"按钮

#### 方法2：测试不同服务器

**旧服务器：**
```
ws://elxn1431783.bohrium.tech:8080/ws
```

**新服务器：**
```
ws://wdzd1450232.bohrium.tech:8080/ws
```

**本地服务器：**
```
ws://localhost:8080/ws
```

**都可以在同一个APK中测试！**

### 🎯 当前APK功能

**已包含的功能：**
- ✅ 服务器地址手动输入
- ✅ WebSocket连接
- ✅ 明文通信支持（ws://）
- ✅ 消息发送/接收
- ✅ 心跳保活（30秒）
- ✅ 自动重连
- ✅ 日志显示和复制
- ✅ 连接状态显示

### 📊 何时需要更新APK

| 操作 | 是否需要更新APK |
|------|----------------|
| 切换服务器地址 | ❌ 不需要 |
| 修改服务器端口 | ❌ 不需要 |
| 修改App UI | ✅ 需要 |
| 修改App功能 | ✅ 需要 |
| 添加新版本功能 | ✅ 需要 |

### 🧪 测试步骤

1. **下载APK**
   - https://github.com/mesunrise/aip/actions
   - 下载最新的 `app-debug`

2. **安装到手机**

3. **输入服务器地址**
   ```
   ws://wdzd1450232.bohrium.tech:8080/ws
   ```

4. **点击连接**

5. **测试功能**
   - 发送消息
   - 查看心跳
   - 测试重连

### ⚠️ 注意事项

**端口问题：**
- 8080端口可能被防火墙阻止
- 建议联系管理员开放端口
- 或修改服务器为80端口

**连接地址格式：**
```
ws://域名或IP:端口/ws
```

**示例：**
- `ws://wdzd1450232.bohrium.tech:8080/ws` ✅
- `ws://192.168.1.100:8080/ws` ✅
- `ws://localhost:8080/ws` ✅

### 🚀 快速测试

**当前可用的服务器：**

1. **新服务器（推荐）**
   ```
   ws://wdzd1450232.bohrium.tech:8080/ws
   ```
   - 状态：✅ 运行中
   - 本地测试：✅ 成功
   - 外网访问：⏳ 需要开放端口

2. **旧服务器**
   ```
   ws://elxn1431783.bohrium.tech:8080/ws
   ```
   - 状态：✅ 运行中
   - 本地测试：✅ 成功
   - 外网访问：❌ 被阻止

### 💡 设计优势

**灵活的服务器配置：**
- 不需要重新编译APK
- 随时切换服务器
- 方便测试和部署
- 支持多种环境

**这就是为什么我们设计成可配置的！**

---

## 总结

**切换服务器 = 在App中修改地址 + 点击连接**

**完全不需要更新APK！** 🎉
